[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
# alembic upgrade head dijalankan dulu setiap deploy (idempoten, aman)
cmd = "cd backend && alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
